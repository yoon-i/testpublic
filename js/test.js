let car, background; let x = 0, y = 0; let mx = 0, my = 0; const speed = 0.03; const loop = () =>{ mx += (x - mx) * speed; my += (y - my) * speed; window.requestAnimationFrame(loop); } const mouseFunc = (e) =>{ x = (e.clientX-window.innerWidth/2), y = (e.clientY-window.innerHeight/2); car.style.transform = `translate(${-(mx/50)}px, ${-(my/50)}px)`; background.style.transform = `translate(${(mx/12)}px, ${(my/6)}px)`; } window.onload = () =>{ car = document.getElementById("car"); background = document.getElementById("background"); loop(); window.addEventListener("mousemove", mouseFunc); }

